# 卡牌資料結構

## 文件目的

本文件定義 P6 中卡牌的**資料模型**——每張卡需要記錄哪些欄位、各欄位的型別與用途、玩家可見性對照、以及實作時的注意事項。本文件是後續卡牌規格設計與程式實作的共同依據。

設計判準:**每個欄位都必須直接服務於某個結算/判斷/操作**。沒有用途的欄位不收錄。

> **本版基於 2026-05-10 改採的「類型計數結算模型」**。舊版倍率模型的資料結構(含 base_strength + traits)已棄用,詳見 [結算機制演化討論.md](結算機制演化討論.md)。

---

## 一、卡牌的兩軸分類

每張卡由**兩條正交獨立的主軸**定義,每張卡都同時填寫兩個軸:

### 軸一:資源類別(描述限制機制與稀有度)

| 類別 | 限制機制 | 設計定位 |
|------|---------|---------|
| 工具 | 有使用次數限制(整備期回補) | 日常戰鬥主力 |
| 爆發 | 單次性消耗(用後移除) | 關鍵時刻決斷 |
| 角色 | 戰役單次(戰役重置) | 招牌動作 / 故事性 |

### 軸二:功能類別(描述卡的核心效果)

| 類別 | 主要功能 | 觸發時機 |
|------|---------|---------|
| 戰鬥收益 | 對敵人需求表貢獻次數 | 結束本擊時整體結算 |
| 情報收益 | 揭露 hidden state | Lock 時觸發 |
| 複合 / 試探 | 輕度次數貢獻 + 輕度揭露 | Lock 時觸發揭露,結束本擊時計算貢獻 |

### 二維展開後的卡牌空間

| | 戰鬥收益 | 情報收益 | 複合 |
|---|---------|---------|------|
| **工具** | 反覆可用的戰鬥卡(資源池形態) | 反覆可用的揭露卡(個體) | 反覆可用的試探卡(個體) |
| **爆發** | 一次性高貢獻(個體) | 一次性大揭露(個體) | 高效試探(個體) |
| **角色** | 招牌大招(個體) | 角色專屬揭露(個體) | 角色專屬複合(個體) |

### 卡牌的「形態」分兩種

新增重要區分:

- **資源池卡**(同質卡):多張完全相同的卡共用同一個 template,只在 `工具 × 戰鬥收益` 象限存在(原型階段)
- **個體卡**(獨特卡):每張卡有獨特設計,涵蓋其餘 8 個象限(以及工具×戰鬥的少數例外)

> 個體卡的存在是為了保留「招牌動作」「特殊效果」「情報投資」等需要獨特性的設計。資源池卡是為了讓玩家專注於「投幾張」的決策而非「選哪張」的記憶負擔。

### 待研究的第四個功能類別:情境收益

跳過卡 / 迴避卡這類「不打也不查,直接跳過」的功能。原型不實作,可能歸類於角色特殊卡類別,待後續研究。

---

## 二、資料欄位完整規範

卡牌資料分三層:

- **卡牌定義**(Card Definition):靜態資料,卡片本身的設計屬性,跨戰役不變
- **卡牌實例池**(Card Instance Pool):戰役進行中的可用張數狀態(資源池卡)
- **卡牌實例狀態**(Card Instance State):戰役進行中的個別卡片狀態(剩餘使用次數等)

### 2.1 卡牌定義(靜態資料)

| 欄位名 | 型別 | 必填 | 用途 | 範例 |
|-------|------|------|------|------|
| `id` | string | ✓ | 系統內部唯一識別 | `"tool_combat_impact_basic"` |
| `name` | string | ✓ | 玩家識別 | `"直擊"` |
| `resource_class` | enum | ✓ | 資源類別軸 | `"tool"` / `"burst"` / `"character"` |
| `function_class` | enum | ✓ | 功能類別軸 | `"combat"` / `"intel"` / `"compound"` |
| `card_form` | enum | ✓ | 卡牌形態 | `"pool"`(資源池) / `"individual"`(個體) |
| `weakness_type` | enum | ✓ | 該卡所屬的弱點類型 | `"impact"` / `"pierce"` / `"burn"` / `"generic"` / `"none"` |
| `contribution` | object \| null | ✓ | 該卡對戰鬥層的次數貢獻 | `{ impact: 2 }` 或 `{ flexible: 2 }` 或 `null` |
| `strength_level` | enum | ✓ | 玩家定性顯示 | `1` / `2` / `3`(對應 ★ ★★ ★★★) |
| `pool_size` | number \| null | △ | 資源池形態時:該池的張數;個體形態時為 null | `4`(直擊池有 4 張) |
| `use_limit` | number | ✓ | 每張卡的使用次數上限 | `3`(★)/ `2`(★★)/ `1`(爆發/角色) |
| `lock_class` | enum | ✓ | Lock 行為類別 | `"none"`(無 lock 需求)/ `"optional"`(可選 lock,改變效果)/ `"required"`(強制 lock 才生效) |
| `special_effect` | object \| null | ✓ | 特殊效果(揭露/影響其他卡等) | 見下節 |
| `description` | string | ✓ | 玩家定性簡述 | `"對單一目標貢獻 +1 衝擊"` |

### lock_class 欄位詳解

決定該卡如何與 Place/Lock 機制互動:

| lock_class | 行為 | 卡面標示 | 適用卡類 |
|-----------|------|---------|---------|
| **`"none"`** | Lock 與否完全不影響效果。可單獨 lock 也可隨「結束本擊」一起 lock | 無 lock 相關標示 | 純戰鬥卡(資源池、爆發戰鬥、角色戰鬥) |
| **`"optional"`** | Lock 與否會**改變揭露對象**:不 lock = 後續連戰廣度;Lock = 當前犧牲廣度換深度 | 卡面寫兩種模式效果 | 情報卡(偵察/估量/警示) |
| **`"required"`** | **必須先 lock 才能生效**;「結束本擊」前所有 placed 的 required 卡必須先 lock | 卡面標 🔒 強制 lock 標示 | 試探卡、flexible 卡、全知之眼 |

**important**:`"required"` 類別的卡,只 place 不 lock 等於沒生效。「結束本擊」按鈕 disabled 直到所有 placed 的 required 卡完成 lock。

### contribution 欄位的詳細說明

`contribution` 是一個 dict,描述本卡對各類型的次數貢獻:

```json
// 純衝擊卡
{ "impact": 1 }

// 強衝擊卡
{ "impact": 2 }

// 複合卡(多類型)
{ "impact": 1, "pierce": 1 }

// 通用 / 玩家可選類型
{ "flexible": 2 }

// 純情報卡(不貢獻次數)
null
```

**flexible 的處理**:當卡片有 `flexible: N` 時,玩家在 commit 時把這 N 次分配到任一類型(可以全押一類,也可以分散)。對應原本「通用」類型的概念。

**多類型貢獻**:角色卡可能有 `{ impact: 2, pierce: 2 }` 這種雙類型貢獻,結算時兩個類型同時各加 2。

### 2.2 卡牌實例池(資源池形態,動態狀態)

對於資源池卡,系統追蹤:

| 欄位名 | 型別 | 範圍 | 用途 |
|-------|------|------|------|
| `card_id` | string | — | 對應卡牌定義 |
| `pool_remaining` | number | 0 ~ pool_size × use_limit | 該池中所有卡牌的剩餘使用次數總和 |

整備期回補時加到 `pool_remaining`(不超過 pool_size × use_limit)。

> 簡化模型:不追蹤池內每張卡的個別狀態,只追蹤總可用次數。例如「直擊池 pool_size=4, use_limit=3」總供給 12 次,玩家用過 3 次後 pool_remaining=9,玩家不需要區分用了哪幾張。

### 2.3 卡牌實例狀態(個體卡,動態狀態)

對於個體卡,系統追蹤:

| 欄位名 | 型別 | 範圍 | 用途 |
|-------|------|------|------|
| `card_id` | string | — | 對應卡牌定義 |
| `remaining_uses` | number | 0 ~ use_limit | 該卡剩餘使用次數 |
| `available` | boolean | — | 該卡當前是否仍在玩家手上(爆發牌用後 = false 移除) |

---

## 三、特殊效果結構

`special_effect` 用於非戰鬥層的效果(揭露、影響其他卡計算等)。

### 觸發時機與 lock_class 的關係

`special_effect` 的觸發機制取決於該卡的 `lock_class`:

- **`lock_class: "none"`**:special_effect 通常為 null(純戰鬥卡無特殊效果)。若有,在「結束本擊」時統一處理。
- **`lock_class: "optional"`**:special_effect 必須有 **`spec_unlocked` + `spec_locked` 雙模式**。不 lock 走 unlocked 模式(在「結束本擊」時觸發);Lock 走 locked 模式(lock 時即時觸發)。
- **`lock_class: "required"`**:special_effect 必須在 lock 時觸發才生效。只 place 不 lock 不生效。

### 結構

#### lock_class 為 "none" 或 "required" 時(單模式)

| 子欄位 | 型別 | 用途 |
|-------|------|------|
| `spec` | object | 機器規格(action + 參數) |
| `display_text` | string | 玩家看到的人話描述 |

#### lock_class 為 "optional" 時(雙模式)

| 子欄位 | 型別 | 用途 |
|-------|------|------|
| `spec_unlocked` | object | 不 lock 時的機器規格 |
| `display_text_unlocked` | string | 不 lock 時的描述 |
| `spec_locked` | object | Lock 時的機器規格 |
| `display_text_locked` | string | Lock 時的描述 |

### 範例

**lock_class: "required" — 揭露下一場敵人弱點類型(對應「不 lock 即不生效」)**
```json
// (此用法已棄用,改為 lock_class: "optional" 的「不 lock 揭露剩餘」模式)
```

**lock_class: "optional" — 偵察的雙模式**
```json
{
  "spec_unlocked": { "action": "reveal_remaining_weakness_types", "scope": "current_battle_chain" },
  "display_text_unlocked": "不 lock:結束本擊時揭露此連戰所有剩餘敵人的弱點類型",
  "spec_locked": { "action": "reveal_weakness_type", "target": "current" },
  "display_text_locked": "Lock:即時揭露當前敵人弱點類型"
}
```

**lock_class: "required" — 試探斬的揭露 + 戰鬥**
```json
{
  "spec": { "action": "reveal_binary", "subject": "is_weakness_impact", "target": "current" },
  "display_text": "Lock 時揭露當前敵人弱點是否為衝擊(yes/no)"
}
```

**lock_class: "required" — 洞察一擊**
```json
{
  "spec": { "action": "reveal_current_weakness", "target": "current" },
  "display_text": "Lock 時揭露當前敵人弱點(本擊內其他未 lock 的 flexible 卡可立即分配對應類型)"
}
```

**對連戰最高需求者的次數加成**
```json
{
  "spec": { "action": "bonus_against_highest", "modifier": "+1_to_each_contribution" },
  "display_text": "若目標是當下連戰中需求最高者,本卡所有貢獻 +1"
}
```

### action 列表(2026-05-11 更新,對應敵人四類分類)

| action | scope/target | 說明 | 使用於 |
|--------|--------------|------|--------|
| `reveal_remaining_weakness_types` | `current_battle_chain` | 揭露此連戰剩餘敵人的弱點類型 | 偵察(不 lock) |
| `reveal_weakness_type` | `current` | 揭露當前敵人弱點類型 | 偵察(lock)、洞察一擊 |
| `reveal_remaining_min_requirements` | `current_battle_chain` | 揭露此連戰剩餘敵人的最低需求次數 | 估量(不 lock) |
| `reveal_full_requirements` | `current` | 揭露當前敵人完整需求表 | 估量(lock) |
| `reveal_remaining_enemy_class` | `current_battle_chain` | 揭露此連戰剩餘敵人的類別(兔子/狼/豹/熊) | 警示(不 lock) |
| `reveal_enemy_class` | `current` | 揭露當前敵人類別 | 警示(lock) |
| `reveal_binary` + `subject: is_weakness_impact` | `current` | 二元揭露:弱點是否為衝擊 | 試探斬 |
| `reveal_binary` + `subject: is_min_req_low` | `current` | 二元揭露:最低需求是否 ≤ 2 | 探刺 |
| `reveal_binary` + `subject: is_failure_game_over` | `current` | 二元揭露:失敗是否會 GAME OVER | 試探擊 |
| `reveal_all_remaining_full` | `current_battle_chain` + includes 陣列 | 揭露剩餘所有敵人完整資訊 | 全知之眼 |
| `reveal_current_weakness` | `current` | 揭露當前敵人弱點(影響本擊內 flexible 卡分配) | 洞察一擊 |
| `bonus_against_highest` | — | 對連戰最高需求者所有貢獻 +1 | 致命凝視 |

**已棄用 action**(配合敵人四類分類取代):
- `reveal_remaining_elite_status_binary` → 取代為 `reveal_remaining_enemy_class`
- `reveal_binary` + `subject: is_elite` → 取代為 `reveal_binary` + `subject: is_failure_game_over`
- 全知之眼 `includes` 中的 `elite_full` → 取代為 `enemy_class`

---

## 四、玩家可見性對照

按呈現層原則:系統內部所有資料都精確,但呈現給玩家分層處理。

| 系統內部資料 | 玩家可見? | 預設緊湊 | 點擊展開 | 教學期額外 | 戰鬥紀錄 |
|------------|---------|---------|---------|---------|---------|
| `id` | ✗ | — | — | — | — |
| `name` | ✓ | 文字(卡面標題) | 同 | — | 同 |
| `resource_class` | ✓ | 圖示或邊框顏色 | 加註類別文字 | — | 同 |
| `function_class` | ✓ | 圖示或邊框顏色 | 加註類別文字 | — | 同 |
| `weakness_type` | ✓ | 圖示+色塊(顯眼) | 加註類型文字 | — | 同 |
| `contribution`(類型) | ✓ | 圖示(顯示是哪一類) | 同 | — | 同 |
| `contribution`(數字) | ✓ | 顯示「+N 衝擊」 | 同 | 同 | 同 |
| `strength_level` | ✓ | ★(醒目) | 同 | — | 同 |
| `card_form` | △ | (隱含於介面分區) | — | — | — |
| `pool_remaining`(池剩餘) | ✓ | 池視覺(填滿狀態) | 數字 | 同 | 同 |
| `remaining_uses`(個體) | ✓ | 點數圖填充(如 ●●○) | 同 + 數字 | 同 | 同 |
| `special_effect.spec` | ✗ | — | — | — | 顯示為文字 |
| `special_effect.display_text` | ✓ | 圖示或關鍵字 | 完整文字 | 同 | 同 |
| `description` | ✓ | 隱藏(緊湊不顯示) | 完整顯示 | 同 | 同 |

**圖例**:✓ 玩家可見 / ✗ 玩家完全不可見 / △ 條件性可見

### 與舊模型的關鍵差別

- **沒有 base_strength 數值**(原本玩家看不到的精確數字),改為 `contribution` 直接顯示「+N 類型」
- **沒有 traits 詞條**(整套刪除)
- **沒有「強度」隱藏精確值的問題**——次數貢獻本身就是定性的(+1 / +2 / +3 對應 ★ / ★★ / ★★★)

---

## 五、教學期 UI 預設的歸屬

「教學期該突顯哪些卡」**不存在卡牌資料中**,而存在每場戰鬥的設定中(對應 [卡牌構思UI處理方案.md](卡牌構思UI處理方案.md))。

實作上,教學期 UI 預設應該存在「戰鬥定義」資料結構中:

```json
{
  "battle_id": "tutorial_1_sentinel",
  "highlighted_pool_types": ["impact", "pierce"],
  "highlighted_individual_cards": [],
  "ui_tools_unlocked": []
}
```

---

## 六、範例:完整填寫(類型計數模型)

### 範例 1:資源池卡 — 直擊(工具 × 戰鬥收益,衝擊池 ★)

```json
{
  "id": "tool_combat_impact_basic",
  "name": "直擊",
  "resource_class": "tool",
  "function_class": "combat",
  "card_form": "pool",
  "weakness_type": "impact",
  "contribution": { "impact": 1 },
  "strength_level": 1,
  "pool_size": 4,
  "use_limit": 3,
  "special_effect": null,
  "description": "對單一目標貢獻 +1 衝擊"
}
```

### 範例 2:資源池卡 — 重斬(工具 × 戰鬥,衝擊池 ★★)

```json
{
  "id": "tool_combat_impact_steady",
  "name": "重斬",
  "resource_class": "tool",
  "function_class": "combat",
  "card_form": "pool",
  "weakness_type": "impact",
  "contribution": { "impact": 2 },
  "strength_level": 2,
  "pool_size": 2,
  "use_limit": 2,
  "special_effect": null,
  "description": "對單一目標貢獻 +2 衝擊"
}
```

### 範例 3:個體卡 — 偵察(工具 × 情報,optional 雙模式)

```json
{
  "id": "intel_weakness",
  "name": "偵察",
  "resource_class": "tool",
  "function_class": "intel",
  "card_form": "individual",
  "weakness_type": "none",
  "contribution": null,
  "strength_level": 1,
  "pool_size": null,
  "use_limit": 2,
  "lock_class": "optional",
  "special_effect": {
    "spec_unlocked": { "action": "reveal_remaining_weakness_types", "scope": "current_battle_chain" },
    "display_text_unlocked": "不 lock:結束本擊時揭露此連戰所有剩餘敵人的弱點類型",
    "spec_locked": { "action": "reveal_weakness_type", "target": "current" },
    "display_text_locked": "Lock:即時揭露當前敵人弱點類型"
  },
  "description": "情報卡。不 lock 換廣度;Lock 換深度"
}
```

### 範例 4:個體卡 — 試探斬(工具 × 複合,required + binary)

```json
{
  "id": "compound_impact",
  "name": "試探斬",
  "resource_class": "tool",
  "function_class": "compound",
  "card_form": "individual",
  "weakness_type": "impact",
  "contribution": { "impact": 1 },
  "strength_level": 1,
  "pool_size": null,
  "use_limit": 3,
  "lock_class": "required",
  "special_effect": {
    "spec": { "action": "reveal_binary", "subject": "is_weakness_impact", "target": "current" },
    "display_text": "Lock 觸發:揭露當前敵人弱點是否為衝擊(yes/no)"
  },
  "description": "試探卡(必 lock)。+1 衝擊 + 驗證當前敵人弱點是否為衝擊"
}
```

### 範例 5:個體卡 — 重擊(爆發 × 戰鬥)

```json
{
  "id": "burst_combat_impact_heavy",
  "name": "重擊",
  "resource_class": "burst",
  "function_class": "combat",
  "card_form": "individual",
  "weakness_type": "impact",
  "contribution": { "impact": 3 },
  "strength_level": 3,
  "pool_size": null,
  "use_limit": 1,
  "special_effect": null,
  "description": "對單一目標貢獻 +3 衝擊,使用後從卡池移除"
}
```

### 範例 6:個體卡 — 殺戮意志(角色 × 戰鬥,複合貢獻)

```json
{
  "id": "character_combat_dual_will",
  "name": "殺戮意志",
  "resource_class": "character",
  "function_class": "combat",
  "card_form": "individual",
  "weakness_type": "generic",
  "contribution": { "impact": 2, "pierce": 2 },
  "strength_level": 3,
  "pool_size": null,
  "use_limit": 1,
  "special_effect": null,
  "description": "戰役招牌:對單一目標同時貢獻 +2 衝擊與 +2 穿刺"
}
```

### 範例 7:個體卡 — 洞察一擊(角色 × 複合,影響本擊計算)

```json
{
  "id": "character_compound_insight",
  "name": "洞察一擊",
  "resource_class": "character",
  "function_class": "compound",
  "card_form": "individual",
  "weakness_type": "generic",
  "contribution": { "flexible": 2 },
  "strength_level": 3,
  "pool_size": null,
  "use_limit": 1,
  "special_effect": {
    "spec": { "action": "reveal_current_weakness", "target": "current" },
    "display_text": "Lock 時揭露當前敵人弱點(本擊內其他未 lock 的 flexible 卡可立即分配對應類型)"
  },
  "description": "戰役招牌:貢獻 +2 任一類型;Lock 時揭露當前弱點影響本擊內後續決策"
}
```

---

## 七、未決細節(待原型實作時處理)

1. **緊湊呈現的 contribution 形式**:用「+1 衝擊」純文字、還是「⚒×1」圖示?
2. **flexible 卡的 commit 介面**:玩家如何分配 flexible 次數?(拖到目標類型按鈕、下拉選擇等)
3. **點擊展開後的展開狀態是否持續**:點開一張展開後,其他卡是否自動回緊湊?
4. **教學期的次數顯示策略**:教學戰役要不要把敵人需求表完全公開?
5. **資源池卡的視覺呈現**:用堆疊卡片視覺、還是「池」狀的計量條?
6. **flexible 與 weakness_type 關係**:flexible 卡的 `weakness_type` 應該設為 `"generic"` 還是 `"none"`?(目前範例用 generic)

---

## 八、與其他文件的關係

- [卡牌構思.md](卡牌構思.md) — 設計原則與分類,本文件實作其資料層
- [卡牌構思UI處理方案.md](卡牌構思UI處理方案.md) — UI 工具與教學期 UI 預設,本文件對應其資料需求
- [設計概念.md](設計概念.md) 第十二章呈現層原則 — 玩家可見性對照的設計依據
- [設計概念.md](設計概念.md) 第十一章單一檢查原則 — 類型計數結算模型的依據
- [戰鬥階段構思.md](戰鬥階段構思.md) 第二章結算分層 — 資料欄位的結算流程對應
- [敵人模板.md](敵人模板.md) — 卡牌的 weakness_type 與 contribution 對應敵人的需求表
- [結算機制演化討論.md](結算機制演化討論.md) — 為什麼採用此資料結構的歷史記錄

---

## 九、版本

- **v0.3** (2026-05-11) — 配合敵人四類分類(兔子/狼/豹/熊)更新 action 列表:新增 reveal_enemy_class / reveal_remaining_enemy_class / reveal_binary(is_failure_game_over);棄用 reveal_remaining_elite_status_binary / is_elite / elite_full
- **v0.2** (2026-05-10) — 配合類型計數結算模型重寫:contribution 取代 base_strength;traits 移除;新增資源池與個體卡的形態區分
- **v0.1** (2026-05-10) — 初版,基於倍率模型(已棄用)
